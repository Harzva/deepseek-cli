# Troubleshooting

Run `provider-api doctor --json`. Run each provider with `--dry-run --json`. For Ollama, ensure the local server is running. For Bedrock, use dry-run until an AWS SigV4 live adapter is configured.
