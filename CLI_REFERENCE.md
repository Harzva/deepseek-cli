# CLI Reference

`provider-api compare`, `provider-api protocols`, `provider-api recommend`, `provider-api skills install --all`, `openai-api responses`, `anthropic-api messages`, `gemini-api generate`, `ollama-api chat`, `bedrock-api converse`.


## API 工具 CLI 与 Agent 编码 CLI

如果你需要区分 `lark-cli` / `openai` CLI 与 Codex CLI / Claude Code 的产品层级，请阅读 [CLI_AGENT_CLI_GUIDE.md](docs/CLI_AGENT_CLI_GUIDE.md)。
