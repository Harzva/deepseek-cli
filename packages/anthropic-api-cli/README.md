# Anthropic / Claude API CLI

Install:

```bash
npm install -g @just-agent/anthropic-api-cli
anthropic-api setup
anthropic-api chat "hello" --dry-run --json
```

This CLI is a script-friendly API client. It does not execute tools or modify local files.
