# GLM / 智谱 BigModel API CLI

Install:

```bash
npm install -g @just-agent/glm-api-cli
```

Use:

```bash
glm setup
glm doctor --json
glm chat "hello" --dry-run --json
glm chat "hello" --json
```

Agent Skill included: `glm-api`.
