# Google Gemini API CLI

Install:

```bash
npm install -g @just-agent/gemini-api-cli
gemini-api chat "hello" --dry-run --json
```

Use `--json` for agents and `--dry-run` for safe request inspection.
