# AWS Bedrock API CLI

Install:

```bash
npm install -g @just-agent/bedrock-api-cli
bedrock-api chat "hello" --dry-run --json
```

Use `--json` for agents and `--dry-run` for safe request inspection.
