# Provider API CLI

Full suite package that installs the `provider-api`, `deepseek`, `kimi`, `mimo`, and `glm` commands.

```bash
npm install -g @just-agent/provider-api-cli
provider-api compare
provider-api skills install --all
```
