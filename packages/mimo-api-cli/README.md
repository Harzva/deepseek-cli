# Xiaomi MiMo API CLI

Install:

```bash
npm install -g @just-agent/mimo-api-cli
```

Use:

```bash
mimo setup
mimo doctor --json
mimo chat "hello" --dry-run --json
mimo chat "hello" --json
```

Agent Skill included: `mimo-api`.
