# OpenAI API CLI

Install:

```bash
npm install -g @just-agent/openai-api-cli
openai-api setup
openai-api chat "hello" --dry-run --json
```

This CLI is a script-friendly API client. It does not execute tools or modify local files.
