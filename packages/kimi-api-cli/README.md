# Kimi / Moonshot API CLI

Install:

```bash
npm install -g @just-agent/kimi-api-cli
```

Use:

```bash
kimi setup
kimi doctor --json
kimi chat "hello" --dry-run --json
kimi chat "hello" --json
```

Agent Skill included: `kimi-api`.
